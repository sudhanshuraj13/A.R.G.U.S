# ARGUS — Raspberry Pi Lightweight Dependencies
# Install with:  pip install -r requirements_pi.txt
#
# Key differences from requirements.txt:
#   - NO Streamlit (saves ~100MB RAM, ~50MB disk)
#   - NO OpenCV — ESP32-CAM sends JPEG over HTTP, captured via requests
#   - RPi.GPIO added for physical button trigger

openai>=1.0.0
SpeechRecognition>=3.10.0
edge-tts>=6.1.0
Pillow>=10.0.0
python-dotenv>=1.0.0
PyAudio>=0.2.14
numpy>=1.24.0
requests>=2.28.0
RPi.GPIO>=0.7.0; sys_platform == 'linux'
ultralytics>=8.0.0
langgraph>=0.2.0
opencv-python>=4.8.0
easyocr>=1.7.0
pytesseract>=0.3.10

